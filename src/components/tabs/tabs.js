import React from 'react'

function Tabs({tabArr}) {
    return (
        <div>
            {tabArr && tabArr.map((item,i)=>{
               return <button>
               </button>
            })}
        </div>
    )
}

export default Tabs
